[package]
name = "nebula-plugin-sdk"
edition.workspace = true
version.workspace = true
description = "NEBULA Plugin SDK — ABI definitions for building plugins"

[lib]
crate-type = ["lib"]

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
